---
pagination_next: null
---

# API Pagination

## Overview

IQM's API supports filtering and sorting in many of its endpoints. By either passing query or request parameters, results can be filtered for certain keywords or entity IDs, or, sorted to a specified order. 

### Filtering and sorting parameters

The Parameters Table below lists the most common filtering and sorting fields used in the IQM API. Their usage will vary based on the endpoint. They will often have a default value if a value is not specified. The default value will be listed as part of an endpoint's documentation if it exists. 

| Parameters |  |
| ---- | --- |
| `ids` <br /><span class="type-text">integer</span> | Filter results by entity ID (varies by endpoint) |
| `searchField` / `keywords` <br /><span class="type-text">string</span> | Search results by keyword |
| `limit` / `noOfEntries` <br /><span class="type-text">integer</span> | Maximum number of entries returned per page |
| `pageNo` / `offset` <br /><span class="type-text">integer</span> | Page number for the returned data |
| `sortBy` <br /><span class="type-text">string</span> | Sorts by ascending (<var>+</var>) or descending (<var>-</var>), the supported values will be listed |


## Examples

### Filter by ID

Many endpoint will support filtering by some kind of entity ID. The exact parameter will vary based on the entity. For example, the [Get List of Campaigns](/Guidelines/Campaign-API-Guidelines#get-list-of-campaigns) endpoint supports filtering by <var>conversionId</var>. When a Conversion ID is passed as a query, the endpoint returns all Campaign IDs that are associated with that Conversion at the top of the results. 

```c title="Query Example"
https://app.iqm.com/api/v2/cmp/campaigns/data?conversionId=18
```

```json title="Response 200"
{
  "statusCode": 200,
  "responseObject": {
    "totalRecords": 1498,
    "data": [
      {
        "id": 176881,
        "name": "Campaign to test duplicate  copy",
        "creativeTypeId": 14,
        "campaignType": "CPV",
        "status": "pending",
        "advertiserId": 100419,
        "created": 1620024156,
        "modifiedDate": 1621571575,
        //highlight-next-line
        "conversionId": 18
      },
      {
        "id": 176880,
        "name": "Campaign to test duplicate update",
        "creativeTypeId": 14,
        "campaignType": "CPV",
        "status": "pending",
        "advertiserId": 100419,
        "created": 1620024050,
        "modifiedDate": 1621571575,
        //highlight-next-line
        "conversionId": 18
      },
      ...
    ],
    "filteredRecords": 1039
  }
}
```

### Filter by Keywords

In this example we will search the results by a keyword. The [Get State Segment](/Guidelines/Master-API-Guidelines#get-state-segment) endpoint uses the POST method, and therefore its pagination parameters will be passed with a JSON formatted request instead of a query. 

```json title="Request Sample"
{
    "searchField": "California"
}
```

```json title="Response 200"
{
    "success": true,
    "data": {
        "data": [
            {
                "id": 30212611,
                "name": "Baja California Sur",
                "parentId": 30100154,
                "geojsonUrl": "https://d3jme5si7t6llb.cloudfront.net/statedata/.geojson"
            },
            {
                "id": 30212610,
                "name": "Baja California",
                "parentId": 30100154,
                "geojsonUrl": "https://d3jme5si7t6llb.cloudfront.net/statedata/.geojson"
            },
            {
                "id": 30200005,
                "name": "California",
                "abbreviation": "CA",
                "parentId": 30100001,
                "geojsonUrl": "https://d3jme5si7t6llb.cloudfront.net/statedata/CA.geojson"
            }
        ],
        "totalRecords": 4964,
        "filteredRecords": 3
    }
}
```

### Filter the Number of Entries

The maximum number of entries returned can be specified with the <var>noOfEntries</var> (or sometimes <var>limit</var>) query parameter. Below we retrieve two entries from the [Get List of Inventories](/Guidelines/Inventory-API-Guide#get-list-of-inventories) endpoint. 

```c title="Query Example"
https://app.iqm.com/api/v3/inv/inventories/list?noOfEntries=2
```

```json title="Response 200"
{
    "success": true,
    "data": {
        "inventoryDataList": [
            {
                "id": 13097078,
                "name": "com.twitter.android",
                "publisher": "adx_pub-3661441281163245",
                "appId": "com.twitter.android",
                "inventoryType": "Android App",
                "impressions": 2698270955,
                "reach": 56406049,
                "videoPercentage": 0.0,
                "displayPercentage": 0.0
            },
            {
                "id": 34520,
                "name": "METROPCS-METROZONE",
                "publisher": "adx_pub-2964924015572549",
                "appId": "com.metropcs.metrozone",
                "inventoryType": "Android App",
                "impressions": 2319549000,
                "reach": 115747223,
                "videoPercentage": 22.769272,
                "displayPercentage": 48.073381
            }
        ]
    }
}
```

### Filter by Page Number 

When the possible number of results exceeds the limit that can be returned, there will be a <var>pageNo</var> parameter to specify which page of results will be returned. Keeping with the example in the previous section, if we append a page number of "2" in addition to the two entries, we'll retrieve the next two records. 

```c title="Query Example"
https://app.iqm.com/api/v3/inv/inventories/list?noOfEntries=2&pageNo=2
```

```json title="Response 200"
{
    "success": true,
    "data": {
        "inventoryDataList": [
            {
                "id": 13106651,
                "name": "X",
                "publisher": "adx_pub-3661441281163245",
                "appId": "333903271",
                "inventoryType": "IOS App",
                "impressions": 1413114383,
                "reach": 57020310,
                "videoPercentage": 0.020887,
                "displayPercentage": 0.0
            },
            {
                "id": 4777647,
                "name": "Samsung TV Plus Free",
                "publisher": "Samsung",
                "appId": "g15147002586",
                "inventoryType": "Other App",
                "impressions": 583064989,
                "reach": 24606677,
                "videoPercentage": 100.0,
                "displayPercentage": 0.0
            }
        ]
    }
}
```

### Sort the Results

