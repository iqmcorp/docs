import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import CopyUrl from '@site/src/components/CopyUrl';

## Healthcare data details

### Healthcare titles list

<CopyUrl method="GET" url="https://api.iqm.com/api/v3/audience/abm/healthcare-data/titles" operationId="getHealthcareTitleList" />

<div class="endpoint-container">
  <div class="child1">

Get a list of Healthcare titles. 

| Query parameters | |
| --- | --- |
| `dataProviderIds` <br /><span class="type-text">array of integers</span> | [Data provider IDs](/guidelines/audience-api/#data-partners-list-for-matched-audience). |
| `pageNo` <br /><span class="type-text">integer</span> | Page number of retrieved data, default: <var>0</var>.
| `noOfEntries` <br /><span class="type-text">integer</span> | Number of entries returned per page, default: <var>50</var>.
| `searchField` <br /><span class="type-text">string</span> | Filter results by search field.
| `audienceSubtypeIds` <br /><span class="type-text">array of integers</span> | List of [audience subtype IDs](/guidelines/audience-api/#audience-subtype-list) to filter the Healthcare titles. Comma-separated string of IDs is also allowed.
| `includeGenericAudience` <br /><span class="type-text">boolean</span> | When <var>true</var>, includes records with no audience subtype alongside any matched subtype IDs. Default: <var>false</var>.

<details className="objectPropertiesDetails" style={{ maxWidth: "800px", padding: "1rem" }}>
 <summary style={{ fontSize: "16px" }}>Response properties</summary>

<table className="objectProperties">
<tr>
  <td>`id` <br /><span class="type-text">integer</span></td>
  <td>Title ID.</td>
</tr>
<tr>
  <td>`displayName` <br /><span class="type-text">string</span></td>
  <td>Title name.</td>
</tr>
<tr>
  <td>`displayOrder` <br /><span class="type-text">integer</span></td>
  <td>Display order.</td>
</tr>
<tr>
  <td>`reach` <br /><span class="type-text">integer</span></td>
  <td>Unique reach for this title.</td>
</tr>
<tr>
  <td>`audienceSubtypeId` <br /><span class="type-text">integer</span></td>
  <td>Audience subtype ID associated with this record.</td>
</tr>
</table>
</details>

</div>
<div class="child2">

<Tabs>
<TabItem value="JSON" label="JSON" default>

```json title="Response 200"
{
  "success": true,
  "data": {
    "titleList": [
      {
        "id": 1,
        "displayName": "Healthcare Title 1",
        "displayOrder": 1,
        "reach": 12345,
        "audienceSubtypeId": 3
      },
      {
        "id": 2,
        "displayName": "Healthcare Title 2",
        "displayOrder": 20,
        "reach": 54321,
        "audienceSubtypeId": null
      }
    ],
    "filteredRecords": 5,
    "totalRecords": 3294
  }
}
```

</TabItem>
<TabItem value="TypeScript" label="TypeScript">

See [TypeScript prerequisites](/getting-started/typescript-prerequisites.mdx) for usage. 

```ts
import {
  getInstance
} from "prerequisites"

const axios = getInstance();

interface Responses {
  200: {
    content: {
      "application/json": {
        success: boolean;
        data: {
          titleList: {
            id: number;
            displayName: string;
            displayOrder: number;
          }[];
          filteredRecords: number;
          totalRecords: number;
        }
      };
    };
  };
}

function getHealthcareTitleList(): Promise<Responses> {
  const options = {
    method: 'GET',
    url: 'https://api.iqm.com/api/v3/audience/abm/healthcare-data/titles',
    params: {
      query: {
        dataProviderIds: `number[]`,
        pageNo?: `number`,
        noOfEntries?: `number`,
        searchField?: `string`,
      }
    }
  }

  return axios.request(options).then(({ data }: { data: Responses }) => data);
}
```

</TabItem></Tabs>

</div></div>

---

### Healthcare specialties list

<CopyUrl method="GET" url="https://api.iqm.com/api/v3/audience/abm/healthcare-data/specialities" operationId="getHealthcareSpecialityList" />

<div class="endpoint-container">
  <div class="child1">

Get a list of Healthcare specialties. 

| Query parameters | |
| --- | --- |
| `dataProviderIds` <br /><span class="type-text">array of integers</span> | [Data provider IDs](/guidelines/audience-api/#data-partners-list-for-matched-audience). |
| `pageNo` <br /><span class="type-text">integer</span> | Page number of retrieved data, default: <var>0</var>.
| `noOfEntries` <br /><span class="type-text">integer</span> | Number of entries returned per page, default: <var>50</var>.
| `searchField` <br /><span class="type-text">string</span> | Filter results by search field.
| `audienceSubtypeIds` <br /><span class="type-text">array of integers</span> | List of [audience subtype IDs](/guidelines/audience-api/#audience-subtype-list) to filter the Healthcare specialties. Comma-separated string of IDs is also allowed.
| `includeGenericAudience` <br /><span class="type-text">boolean</span> | When <var>true</var>, includes records with no audience subtype alongside any matched subtype IDs. Default: <var>false</var>.

<details className="objectPropertiesDetails" style={{ maxWidth: "800px", padding: "1rem" }}>
 <summary style={{ fontSize: "16px" }}>Response properties</summary>

<table className="objectProperties">
<tr>
  <td>`id` <br /><span class="type-text">integer</span></td>
  <td>Specialty ID.</td>
</tr>
<tr>
  <td>`displayName` <br /><span class="type-text">string</span></td>
  <td>Specialty name.</td>
</tr>
<tr>
  <td>`displayOrder` <br /><span class="type-text">integer</span></td>
  <td>Display order.</td>
</tr>
<tr>
  <td>`reach` <br /><span class="type-text">integer</span></td>
  <td>Unique reach for this specialty.</td>
</tr>
<tr>
  <td>`audienceSubtypeId` <br /><span class="type-text">integer</span></td>
  <td>Audience subtype ID associated with this record.</td>
</tr>
</table>
</details>

</div>
<div class="child2">

<Tabs>
<TabItem value="JSON" label="JSON" default>

```json title="Response 200"
{
  "success": true,
  "data": {
    "specialityList": [
      {
        "id": 1,
        "displayName": "Healthcare Speciality 1",
        "displayOrder": 1,
        "reach": 12345,
        "audienceSubtypeId": 3
      },
      {
        "id": 2,
        "displayName": "Healthcare Speciality 2",
        "displayOrder": 20,
        "reach": 54321,
        "audienceSubtypeId": null
      }
    ],
    "filteredRecords": 5,
    "totalRecords": 3294
  }
}
```

</TabItem>
<TabItem value="TypeScript" label="TypeScript">

See [TypeScript prerequisites](/getting-started/typescript-prerequisites.mdx) for usage. 

```ts
import {
  getInstance
} from "prerequisites"

const axios = getInstance();

interface Responses {
  200: {
    content: {
      "application/json": {
        success: boolean;
        data: {
          specialityList: {
            id: number;
            displayName: string;
            displayOrder: number;
          }[];
          filteredRecords: number;
          totalRecords: number;
        }
      };
    };
  };
}

function getHealthcareSpecialityList(): Promise<Responses> {
  const options = {
    method: 'GET',
    url: 'https://api.iqm.com/api/v3/audience/abm/healthcare-data/specialities',
    params: {
      query: {
        dataProviderIds: `number[]`,
        pageNo?: `number`,
        noOfEntries?: `number`,
        searchField?: `string`,
      }
    }
  }

  return axios.request(options).then(({ data }: { data: Responses }) => data);
}
```

</TabItem></Tabs>

</div></div>

---

### Healthcare systems list

<CopyUrl method="GET" url="https://api.iqm.com/api/v3/audience/abm/healthcare-data/healthcare-systems" operationId="getHealthcareSystemList" />

<div class="endpoint-container">
  <div class="child1">

Get a list of Healthcare systems. 

| Query parameters | |
| --- | --- |
| `dataProviderIds` <br /><span class="type-text">array of integers</span> | [Data provider IDs](/guidelines/audience-api/#data-partners-list-for-matched-audience). |
| `pageNo` <br /><span class="type-text">integer</span> | Page number of retrieved data, default: <var>0</var>.
| `noOfEntries` <br /><span class="type-text">integer</span> | Number of entries returned per page, default: <var>50</var>.
| `searchField` <br /><span class="type-text">string</span> | Filter results by search field.
| `audienceSubtypeIds` <br /><span class="type-text">array of integers</span> | List of [audience subtype IDs](/guidelines/audience-api/#audience-subtype-list) to filter the Healthcare systems. Comma-separated string of IDs is also allowed.
| `includeGenericAudience` <br /><span class="type-text">boolean</span> | When <var>true</var>, includes records with no audience subtype alongside any matched subtype IDs. Default: <var>false</var>.

<details className="objectPropertiesDetails" style={{ maxWidth: "800px", padding: "1rem" }}>
 <summary style={{ fontSize: "16px" }}>Response properties</summary>

<table className="objectProperties">
<tr>
  <td>`id` <br /><span class="type-text">integer</span></td>
  <td>System ID.</td>
</tr>
<tr>
  <td>`displayName` <br /><span class="type-text">string</span></td>
  <td>System name.</td>
</tr>
<tr>
  <td>`displayOrder` <br /><span class="type-text">integer</span></td>
  <td>Display order.</td>
</tr>
<tr>
  <td>`reach` <br /><span class="type-text">integer</span></td>
  <td>Unique reach for this system.</td>
</tr>
<tr>
  <td>`audienceSubtypeId` <br /><span class="type-text">integer</span></td>
  <td>Audience subtype ID associated with this record.</td>
</tr>
</table>
</details>

</div>
<div class="child2">

<Tabs>
<TabItem value="JSON" label="JSON" default>

```json title="Response 200"
{
  "success": true,
  "data": {
    "healthcareSystemList": [
      {
        "id": 1,
        "displayName": "Healthcare System 1",
        "displayOrder": 1,
        "reach": 1000,
        "audienceSubtypeId": 3
      },
      {
        "id": 2,
        "displayName": "Healthcare System 2",
        "displayOrder": 20,
        "reach": 2000,
        "audienceSubtypeId": 3
      }
    ],
    "filteredRecords": 5,
    "totalRecords": 3294
  }
}
```

</TabItem>
<TabItem value="TypeScript" label="TypeScript">

See [TypeScript prerequisites](/getting-started/typescript-prerequisites.mdx) for usage. 

```ts
import {
  getInstance
} from "prerequisites"

const axios = getInstance();

interface Responses {
  200: {
    content: {
      "application/json": {
        success: boolean;
        data: {
          healthcareSystemList: {
            id: number;
            displayName: string;
            displayOrder: number;
          }[];
          filteredRecords: number;
          totalRecords: number;
        }
      };
    };
  };
}

function getHealthcareSystemList(): Promise<Responses> {
  const options = {
    method: 'GET',
    url: 'https://api.iqm.com/api/v3/audience/abm/healthcare-data/healthcare-systems',
    params: {
      query: {
        dataProviderIds: `number[]`,
        pageNo?: `number`,
        noOfEntries?: `number`,
        searchField?: `string`,
      }
    }
  }

  return axios.request(options).then(({ data }: { data: Responses }) => data);
}
```

</TabItem></Tabs>

</div></div>

---

### Healthcare data

<CopyUrl method="POST" url="https://api.iqm.com/api/v3/audience/abm/healthcare-data" operationId="getHealthcareData" />

<div class="endpoint-container">
  <div class="child1">

Get a Healthcare data list by specified filters. 

| Query parameters | |
| --- | --- |
| `dataProviderIds` <br /><span class="type-text">array of integers</span> | [Data provider IDs](/guidelines/audience-api/#data-partners-list-for-matched-audience). |
| `pageNo` <br /><span class="type-text">integer</span> | Page number of retrieved data, default: <var>0</var>.
| `noOfEntries` <br /><span class="type-text">integer</span> | Number of entries returned per page, default: <var>50</var>.
| `healthCareSystemIds` <br /><span class="type-text">array of integers </span> | [Healthcare system IDs](#healthcare-systems-list).
| `accountNameIds` <br /><span class="type-text">array of integers </span> | [Healthcare account name IDs](#healthcare-account-names).
| `accountTypeIds` <br /><span class="type-text">array of integers </span> | [Healthcare account type IDs](#healthcare-account-types). 
| `accountSubTypeIds` <br /><span class="type-text">array of integers </span> | [Healthcare account subtype IDs](#healthcare-account-subtypes).
| `countryIds` <br /><span class="type-text">array of integers </span> | [Country IDs](/guidelines/master-api#get-country-segment). 
| `stateIds` <br /><span class="type-text">array of integers </span> | [State IDs](/guidelines/master-api#get-state-segment).
| `cityIds` <br /><span class="type-text">array of integers </span> | [City IDs](/guidelines/master-api#get-city-segment).
| `titleIds` <br /><span class="type-text">array of integers </span> | [Healthcare title IDs](#healthcare-titles-list).
| `specialityIds` <br /><span class="type-text">array of integers </span> | [Healthcare specialty IDs](#healthcare-specialties-list). 
| `ids` <br /><span class="type-text">array of integers </span> | IDs.
| `audienceSubtypeIds` <br /><span class="type-text">array of integers</span> | [Audience subtype IDs](/guidelines/audience-api/#audience-subtype-list) to filter the records.
| `includeGenericAudience` <br /><span class="type-text">boolean</span> | When <var>true</var>, includes records with no audience subtype alongside any matched subtype IDs. Default: <var>false</var>.

<details className="objectPropertiesDetails" style={{ maxWidth: "800px", padding: "1rem" }}>
 <summary style={{ fontSize: "16px" }}>Response properties</summary>

<table className="objectProperties">
<tr>
  <td>`id` <br /><span class="type-text">integer</span></td>
  <td>Audience ID.</td>
</tr>
<tr>
  <td>`cost` <br /><span class="type-text">integer</span></td>
  <td>Charges for a specific segment.</td>
</tr>
<tr>
  <td>`reach` <br /><span class="type-text">integer</span></td>
  <td>Unique reach of segments (i.e., audience reach).</td>
</tr>
<tr>
  <td>`healthCareSystemId` <br /><span class="type-text">integer</span></td>
  <td>[Healthcare system ID](#healthcare-systems-list).</td>
</tr>
<tr>
  <td>`healthCareSystemName` <br /><span class="type-text">string</span></td>
  <td>Healthcare system name.</td>
</tr>
<tr>
  <td>`accountId` <br /><span class="type-text">integer</span></td>
  <td>[Healthcare account ID](#healthcare-account-names).</td>
</tr>
<tr>
  <td>`accountName` <br /><span class="type-text">string</span></td>
  <td>Healthcare account name.</td>
</tr>
<tr>
  <td>`accountTypeId` <br /><span class="type-text">integer</span></td>
  <td>[Healthcare account type ID](#healthcare-account-types).</td>
</tr>
<tr>
  <td>`accountTypeName` <br /><span class="type-text">string</span></td>
  <td>Healthcare account type name.</td>
</tr>
<tr>
  <td>`accountSubTypeId` <br /><span class="type-text">integer</span></td>
  <td>[Healthcare account subtype ID](#healthcare-account-subtypes).</td>
</tr>
<tr>
  <td>`accountSubTypeName` <br /><span class="type-text">string</span></td>
  <td>Healthcare account subtype name.</td>
</tr>
<tr>
  <td>`titleId` <br /><span class="type-text">integer</span></td>
  <td>[Healthcare title ID](#healthcare-titles-list).</td>
</tr>
<tr>
  <td>`titleName` <br /><span class="type-text">string</span></td>
  <td>Healthcare title name.</td>
</tr>
<tr>
  <td>`specialityId` <br /><span class="type-text">integer</span></td>
  <td>[Healthcare specialty ID](#healthcare-specialties-list).</td>
</tr>
<tr>
  <td>`specialityName` <br /><span class="type-text">string</span></td>
  <td>Healthcare specialty name.</td>
</tr>
<tr>
  <td>`countryId` <br /><span class="type-text">integer</span></td>
  <td>[Country ID](/guidelines/master-api#get-country-segment).</td>
</tr>
<tr>
  <td>`countryName` <br /><span class="type-text">string</span></td>
  <td>Country name.</td>
</tr>
<tr>
  <td>`stateId` <br /><span class="type-text">integer</span></td>
  <td>[State ID](/guidelines/master-api#get-state-segment).</td>
</tr>
<tr>
  <td>`stateName` <br /><span class="type-text">string</span></td>
  <td>State name.</td>
</tr>
<tr>
  <td>`cityId` <br /><span class="type-text">integer</span></td>
  <td>[City ID](/guidelines/master-api#get-city-segment).</td>
</tr>
<tr>
  <td>`cityName` <br /><span class="type-text">string</span></td>
  <td>City name.</td>
</tr>
<tr>
  <td>`dataProviderId` <br /><span class="type-text">integer</span></td>
  <td>[Data provider ID](/guidelines/audience-api/#data-partners-list-for-matched-audience).</td>
</tr>
<tr>
  <td>`dataProviderName` <br /><span class="type-text">string</span></td>
  <td>Data provider name.</td>
</tr>
</table>
</details>

</div>
<div class="child2">

<Tabs>
<TabItem value="JSON" label="JSON" default>

```json title="Request sample"
{
  "dataProviderIds": [
    1
  ],
  "healthCareSystemIds": [
    40100004,
    40100007
  ],
  "accountIds": [
    40208104,
    40628423
  ],
  "accountTypeIds": [
    40300003
  ],
  "accountSubTypeIds": [
    40400001,
    40400010
  ],
  "countryIds": [
    30100001
  ],
  "stateIds": [
    30200028,
    30200043
  ],
  "cityIds": [
    30430356,
    30447825,
    30447952,
    30448122
  ],
  "titleIds": [
    40500099
  ],
  "specialityIds": [
    40500014,
    40500086
  ],
  "ids": [
    1,
    2,
    3
  ],
  "pageNo": 1,
  "noOfEntries": 2,
  "audienceSubtypeIds": [3],
  "includeGenericAudience": false
}
```

```json title="Response 200"
{
  "success": true,
  "data": {
    "totalRecords": 52265,
    "filteredRecords": 5,
    "healthCareData": [
      {
        "id": 1,
        "cost": 10,
        "reach": 1,
        "healthCareSystemId": 40100004,
        "healthCareSystemName": "ABCD PEDIATRICS",
        "accountId": 40628423,
        "accountName": "Stepping Stone Pediatrics PA",
        "accountTypeId": 40300003,
        "accountTypeName": "Group Practice",
        "accountSubTypeId": 40400010,
        "accountSubTypeName": "Telephone/E-Mail Order Pharmacy",
        "titleId": 40500099,
        "titleName": "MD",
        "specialityId": 40500086,
        "specialityName": "Gastroenterology",
        "countryId": 30100001,
        "countryName": "United States",
        "stateId": 30200043,
        "stateName": "Tennessee",
        "cityId": 30447952,
        "cityName": "Franklin, TN",
        "dataProviderId": 1,
        "dataProviderName": "HealthCarelink",
        "audienceSubtypeId": 3
      },
      {
        "id": 2,
        "cost": 10,
        "reach": 1,
        "healthCareSystemId": 40100004,
        "healthCareSystemName": "ABCD PEDIATRICS",
        "accountId": 40628423,
        "accountName": "Stepping Stone Pediatrics PA",
        "accountTypeId": 40300003,
        "accountTypeName": "Group Practice",
        "accountSubTypeId": 40400010,
        "accountSubTypeName": "Telephone/E-Mail Order Pharmacy",
        "titleId": 40500099,
        "titleName": "MD",
        "specialityId": 40500086,
        "specialityName": "Gastroenterology",
        "countryId": 30100001,
        "countryName": "United States",
        "stateId": 30200043,
        "stateName": "Tennessee",
        "cityId": 30447825,
        "cityName": "Brentwood, TN",
        "dataProviderId": 1,
        "dataProviderName": "HealthCarelink",
        "audienceSubtypeId": 3
      }
    ]
  }
}
```

</TabItem>
<TabItem value="TypeScript" label="TypeScript">

See [TypeScript prerequisites](/getting-started/typescript-prerequisites.mdx) for usage. 

```ts
import {
  getInstance
} from "prerequisites"

const axios = getInstance();

interface Responses {
  200: {
    content: {
      "application/json": {
        success: boolean;
        data: {
          totalRecords: number;
          filteredRecords: number;
          healthCareData: {
            id: number;
            cost: number;
            reach: number;
            healthCareSystemId: number;
            healthCareSystemName: string;
            accountId: number;
            accountName: string;
            accountTypeId: number;
            accountTypeName: string;
            accountSubTypeId: number;
            accountSubTypeName: string;
            titleId: number;
            titleName: string;
            specialityId: number;
            specialityName: string;
            countryId: number;
            countryName: string;
            stateId: number;
            stateName: string;
            cityId: number;
            cityName: string;
            dataProviderId: number;
            dataProviderName: string;
          }[];
        }
      };
    };
  };
}

function getHealthCareData(): Promise<Responses> {
  const options = {
    method: 'POST',
    url: 'https://api.iqm.com/api/v3/audience/abm/healthcare-data',
    requestBody: {
      content: {
        "application/json": {
          searchBy?: `string[]`,
          ids?: `number[]`,
          dataProviderIds?: `number[]`,
          healthCareSystemIds?: `number[]`,
          accountIds?: `number[]`,
          accountTypeIds?: `number[]`,
          accountSubTypeIds?: `number[]`,
          titleIds?: `number[]`,
          specialityIds?: `number[]`,
          countryIds?: `number[]`,
          stateIds?: `number[]`,
          cityIds?: `number[]`,
          pagination?: `string`,
          pageNo?: `number`,
          noOfEntries?: `number`,
          sortBy?: `string`,
          searchField?: `string`,
          order?: `ASC` | `DESC`,
          offset?: `number`,
        }
      }
    }
  }

  return axios.request(options).then(({ data }: { data: Responses }) => data);
}
```

</TabItem></Tabs>

</div></div>

---

### Healthcare statistics

<CopyUrl method="POST" url="https://api.iqm.com/api/v3/audience/abm/healthcare-data/statistics" operationId="getHealthcareDataStatistics" />

<div class="endpoint-container">
  <div class="child1">

Get Healthcare statistics details. 

| Query parameters | |
| --- | --- |
| `dataProviderIds` <br /><span class="type-text">array of integers</span> | [Data provider IDs](/guidelines/audience-api/#data-partners-list-for-matched-audience). |
| `pageNo` <br /><span class="type-text">integer</span> | Page number of retrieved data, default: <var>0</var>.
| `noOfEntries` <br /><span class="type-text">integer</span> | Number of entries returned per page, default: <var>50</var>.
| `healthCareSystemIds` <br /><span class="type-text">array of integers </span> | Healthcare system IDs. 
| `accountNameIds` <br /><span class="type-text">array of integers </span> | Account name IDs. 
| `accountTypeIds` <br /><span class="type-text">array of integers </span> | [Healthcare account type IDs](#healthcare-account-types). 
| `accountSubTypeIds` <br /><span class="type-text">array of integers </span> | Account subtype IDs. 
| `countryIds` <br /><span class="type-text">array of integers </span> | [Country IDs](/guidelines/master-api#get-country-segment). 
| `stateIds` <br /><span class="type-text">array of integers </span> | [State IDs](/guidelines/master-api#get-state-segment).
| `cityIds` <br /><span class="type-text">array of integers </span> | [City IDs](/guidelines/master-api#get-city-segment). 
| `titleIds` <br /><span class="type-text">array of integers </span> | Title IDs. 
| `specialityIds` <br /><span class="type-text">array of integers </span> | [Healthcare specialty IDs](#healthcare-specialties-list). 
| `ids` <br /><span class="type-text">array of integers </span> | IDs.
| `audienceSubtypeIds` <br /><span class="type-text">array of integers</span> | [Audience subtype IDs](/guidelines/audience-api/#audience-subtype-list) to filter the records.
| `includeGenericAudience` <br /><span class="type-text">boolean</span> | When <var>true</var>, includes records with no audience subtype alongside any matched subtype IDs. Default: <var>false</var>.

<details className="objectPropertiesDetails" style={{ maxWidth: "800px", padding: "1rem" }}>
 <summary style={{ fontSize: "16px" }}>Response properties</summary>

<table className="objectProperties">
<tr>
  <td>`healthSystem` <br /><span class="type-text">integer</span></td>
  <td>[Healthcare system ID](#healthcare-systems-list).</td>
</tr>
<tr>
  <td>`account` <br /><span class="type-text">integer</span></td>
  <td>[Healthcare account ID](#healthcare-account-names).</td>
</tr>
<tr>
  <td>`accountType` <br /><span class="type-text">integer</span></td>
  <td>[Healthcare account type ID](#healthcare-account-types).</td>
</tr>
<tr>
  <td>`npiReach` <br /><span class="type-text">integer</span></td>
  <td>NPI reach.</td>
</tr>
<tr>
  <td>`speciality` <br /><span class="type-text">integer</span></td>
  <td>[Healthcare specialty ID](#healthcare-specialties-list).</td>
</tr>
</table>
</details>

</div>
<div class="child2">

<Tabs>
<TabItem value="JSON" label="JSON" default>

```json title="Request sample"
{
  "dataProviderIds": [
    1
  ],
  "healthCareSystemIds": [
    40100004,
    40100007
  ],
  "accountIds": [
    40208104,
    40628423
  ],
  "accountTypeIds": [
    40300003
  ],
  "accountSubTypeIds": [
    40400001,
    40400010
  ],
  "countryIds": [
    30100001
  ],
  "stateIds": [
    30200028,
    30200043
  ],
  "cityIds": [
    30430356,
    30447825,
    30447952,
    30448122
  ],
  "titleIds": [
    40500099
  ],
  "specialityIds": [
    40500014,
    40500086
  ],
  "ids": [
    1,
    2,
    3
  ],
  "pageNo": 1,
  "noOfEntries": 2,
  "audienceSubtypeIds": [3],
  "includeGenericAudience": false
}
```

```json title="Response 200"
{
  "success": true,
  "data": {
    "healthSystem": 2,
    "account": 4,
    "accountType": 8,
    "npiReach": 25513,
    "speciality": 3
  }
}
```

</TabItem>
<TabItem value="TypeScript" label="TypeScript">

See [TypeScript prerequisites](/getting-started/typescript-prerequisites.mdx) for usage. 

```ts
import {
  getInstance
} from "prerequisites"

const axios = getInstance();

interface Responses {
  200: {
    content: {
      "application/json": {
        success: boolean;
        data: {
          healthSystem: number;
          account: number;
          accountType: number;
          npiReach: number;
          speciality: number;
        }
      };
    };
  };
}

function getHealthCareDataStatistics(): Promise<Responses> {
  const options = {
    method: 'POST',
    url: 'https://api.iqm.com/api/v3/audience/abm/healthcare-data/statistics',
    requestBody: {
      content: {
        "application/json": {
          searchBy?: `string[]`,
          ids?: `number[]`,
          dataProviderIds?: `number[]`,
          healthCareSystemIds?: `number[]`,
          accountIds?: `number[]`,
          accountTypeIds?: `number[]`,
          accountSubTypeIds?: `number[]`,
          titleIds?: `number[]`,
          specialityIds?: `number[]`,
          countryIds?: `number[]`,
          stateIds?: `number[]`,
          cityIds?: `number[]`,
          pagination?: `string`,
          pageNo?: `number`,
          noOfEntries?: `number`,
          sortBy?: `string`,
          searchField?: `string`,
          order?: `ASC` | `DESC`,
          offset?: `number`,
        }
      }
    }
  }

  return axios.request(options).then(({ data }: { data: Responses }) => data);
}
```

</TabItem></Tabs>

</div></div>

---

### Healthcare account types

<CopyUrl method="POST" url="https://api.iqm.com/api/v3/audience/abm/healthcare-data/account-types" operationId="getAccountTypeList" />

<div class="endpoint-container">
  <div class="child1">

Get a list of Healthcare account types.

| Query parameters | |
| --- | --- |
| `dataProviderIds` <br /><span class="type-text">array of integers</span> | [Data provider IDs](/guidelines/audience-api/#data-partners-list-for-matched-audience). |
| `pageNo` <br /><span class="type-text">integer</span> | Page number of retrieved data, default: <var>0</var>.
| `noOfEntries` <br /><span class="type-text">integer</span> | Number of entries returned per page, default: <var>50</var>.
| `healthCareSystemIds` <br /><span class="type-text">array of integers </span> | Healthcare system IDs. 
| `accountNameIds` <br /><span class="type-text">array of integers </span> | Account name IDs. 
| `searchField` <br /><span class="type-text">string</span> | Filter results by search field.
| `audienceSubtypeIds` <br /><span class="type-text">array of integers</span> | [Audience subtype IDs](/guidelines/audience-api/#audience-subtype-list) to filter the records.
| `includeGenericAudience` <br /><span class="type-text">boolean</span> | When <var>true</var>, includes records with no audience subtype alongside any matched subtype IDs. Default: <var>false</var>.

<details className="objectPropertiesDetails" style={{ maxWidth: "800px", padding: "1rem" }}>
 <summary style={{ fontSize: "16px" }}>Response properties</summary>

<table className="objectProperties">
<tr>
  <td>`id` <br /><span class="type-text">integer</span></td>
  <td>Account ID.</td>
</tr>
<tr>
  <td>`audienceSubtypeId` <br /><span class="type-text">integer</span></td>
  <td>Audience subtype ID associated with this record.</td>
</tr>
<tr>
  <td>`displayName` <br /><span class="type-text">string</span></td>
  <td>Account name.</td>
</tr>
<tr>
  <td>`displayOrder` <br /><span class="type-text">integer</span></td>
  <td>Display order.</td>
</tr>
</table>
</details>

</div>
<div class="child2">

<Tabs>
<TabItem value="JSON" label="JSON" default>

```json title="Request sample"
{
  "pageNo": 1,
  "noOfEntries": 20,
  "searchField": "Sample Text",
  "dataProviderIds": [
    1
  ],
  "healthcareSystemIds": [
    1,
    2,
    3
  ],
  "accountNameIds": [
    4,
    5,
    6
  ],
  "audienceSubtypeIds": [3],
  "includeGenericAudience": false
}
```

```json title="Response 200"
{
  "success": true,
  "data": {
    "accountTypeList": [
      {
        "id": 1,
        "displayName": "Account Type 1",
        "displayOrder": 1,
        "audienceSubtypeId": 3
      },
      {
        "id": 2,
        "displayName": "Account Type 2",
        "displayOrder": 2,
        "audienceSubtypeId": null
      }
    ],
    "filteredRecords": 5,
    "totalRecords": 3294
  }
}
```

</TabItem>
<TabItem value="TypeScript" label="TypeScript">

See [TypeScript prerequisites](/getting-started/typescript-prerequisites.mdx) for usage. 

```ts
import {
  getInstance
} from "prerequisites"

const axios = getInstance();

interface Responses {
  200: {
    content: {
      "application/json": {
        success: boolean;
        data: {
          accountTypeList: {
            id: number;
            displayName: string;
            displayOrder: number;
          }[];
          filteredRecords: number;
          totalRecords: number;
        }
      };
    };
  };
}

function getAccountTypeList(): Promise<Responses> {
  const options = {
    method: 'POST',
    url: 'https://api.iqm.com/api/v3/audience/abm/healthcare-data/account-types',
    requestBody: {
      content: {
        "application/json": {
          searchBy?: `string[]`,
          ids?: `number[]`,
          dataProviderIds?: `number[]`,
          verticalId: `number`,
          healthCareSystemIds?: `number[]`,
          accountNameIds?: `number[]`,
          accountTypeIds?: `number[]`,
          pagination?: `string`,
          pageNo?: `number`,
          noOfEntries?: `number`,
          sortBy?: `string`,
          searchField?: `string`,
          order?: `ASC` | `DESC`,
          offset?: `number`,
        }
      }
    }
  }

  return axios.request(options).then(({ data }: { data: Responses }) => data);
}
```

</TabItem></Tabs>

</div></div>

---

### Healthcare account subtypes

<CopyUrl method="POST" url="https://api.iqm.com/api/v3/audience/abm/healthcare-data/account-subtypes" operationId="getAccountSubTypeList" />

<div class="endpoint-container">
  <div class="child1">

Get a list of Healthcare account subtypes. 

| Query parameters | |
| --- | --- |
| `dataProviderIds` <br /><span class="type-text">array of integers</span> | [Data provider IDs](/guidelines/audience-api/#data-partners-list-for-matched-audience). |
| `pageNo` <br /><span class="type-text">integer</span> | Page number of retrieved data, default: <var>0</var>.
| `noOfEntries` <br /><span class="type-text">integer</span> | Number of entries returned per page, default: <var>50</var>.
| `healthCareSystemIds` <br /><span class="type-text">array of integers </span> | Healthcare system IDs. 
| `accountNameIds` <br /><span class="type-text">array of integers </span> | Account name IDs. 
| `searchField` <br /><span class="type-text">string</span> | Filter results by search field.
| `accountTypeIds` <br /><span class="type-text">array of integers </span> | [Healthcare account type IDs](#healthcare-account-types).
| `audienceSubtypeIds` <br /><span class="type-text">array of integers</span> | [Audience subtype IDs](/guidelines/audience-api/#audience-subtype-list) to filter the records.
| `includeGenericAudience` <br /><span class="type-text">boolean</span> | When <var>true</var>, includes records with no audience subtype alongside any matched subtype IDs. Default: <var>false</var>.

<details className="objectPropertiesDetails" style={{ maxWidth: "800px", padding: "1rem" }}>
 <summary style={{ fontSize: "16px" }}>Response properties</summary>

<table className="objectProperties">
<tr>
  <td>`id` <br /><span class="type-text">integer</span></td>
  <td>Account subtype ID.</td>
</tr>
<tr>
  <td>`audienceSubtypeId` <br /><span class="type-text">integer</span></td>
  <td>Audience subtype ID associated with this record.</td>
</tr>
<tr>
  <td>`displayName` <br /><span class="type-text">string</span></td>
  <td>Account sub name.</td>
</tr>
<tr>
  <td>`displayOrder` <br /><span class="type-text">integer</span></td>
  <td>Display order.</td>
</tr>
</table>
</details>

</div>
<div class="child2">

<Tabs>
<TabItem value="JSON" label="JSON" default>

```json title="Request sample"
{
  "pageNo": 1,
  "noOfEntries": 20,
  "searchField": "Sample Text",
  "dataProviderIds": [
    1
  ],
  "healthcareSystemIds": [
    1,
    2,
    3
  ],
  "accountNameIds": [
    4,
    5,
    6
  ],
  "accountTypeIds": [
    7,
    8,
    9
  ],
  "audienceSubtypeIds": [3],
  "includeGenericAudience": true
}
```

```json title="Response 200"
{
  "success": true,
  "data": {
    "accountSubTypeList": [
      {
        "id": 1,
        "displayName": "Account Subtype 1",
        "displayOrder": 1,
        "audienceSubtypeId": 3
      },
      {
        "id": 2,
        "displayName": "Account Subtype 2",
        "displayOrder": 2,
        "audienceSubtypeId": null
      }
    ],
    "filteredRecords": 5,
    "totalRecords": 3294
  }
}
```

</TabItem>
<TabItem value="TypeScript" label="TypeScript">

See [TypeScript prerequisites](/getting-started/typescript-prerequisites.mdx) for usage. 

```ts
import {
  getInstance
} from "prerequisites"

const axios = getInstance();

interface Responses {
  200: {
    content: {
      "application/json": {
        success: boolean;
        data: {
          accountSubTypeList: {
            id: number;
            displayName: string;
            displayOrder: number;
          }[];
          filteredRecords: number;
          totalRecords: number;
        }
      };
    };
  };
}

function getAccountSubTypeList(): Promise<Responses> {
  const options = {
    method: 'POST',
    url: 'https://api.iqm.com/api/v3/audience/abm/healthcare-data/account-subtypes',
    requestBody: {
      content: {
        "application/json": {
          searchBy?: `string[]`,
          ids?: `number[]`,
          dataProviderIds?: `number[]`,
          verticalId: `number`,
          healthCareSystemIds?: `number[]`,
          accountNameIds?: `number[]`,
          accountTypeIds?: `number[]`,
          pagination?: `string`,
          pageNo?: `number`,
          noOfEntries?: `number`,
          sortBy?: `string`,
          searchField?: `string`,
          order?: `ASC` | `DESC`,
          offset?: `number`,
        }
      }
    }
  }

  return axios.request(options).then(({ data }: { data: Responses }) => data);
}
```

</TabItem></Tabs>

</div></div>

---

### Healthcare account names

<CopyUrl method="POST" url="https://api.iqm.com/api/v3/audience/abm/healthcare-data/account-names" operationId="getAccountNameList" />

<div class="endpoint-container">
  <div class="child1">

Get a list of Healthcare account names. 

| Query parameters | |
| --- | --- |
| `dataProviderIds` <br /><span class="type-text">array of integers</span> | [Data provider IDs](/guidelines/audience-api/#data-partners-list-for-matched-audience). |
| `pageNo` <br /><span class="type-text">integer</span> | Page number of retrieved data, default: <var>0</var>.
| `noOfEntries` <br /><span class="type-text">integer</span> | Number of entries returned per page, default: <var>50</var>.
| `healthCareSystemIds` <br /><span class="type-text">array of integers </span> | Healthcare system IDs. 
| `searchField` <br /><span class="type-text">string</span> | Filter results by search field.

<details className="objectPropertiesDetails" style={{ maxWidth: "800px", padding: "1rem" }}>
 <summary style={{ fontSize: "16px" }}>Response properties</summary>

<table className="objectProperties">
<tr>
  <td>`id` <br /><span class="type-text">integer</span></td>
  <td>Account name ID.</td>
</tr>
<tr>
  <td>`displayName` <br /><span class="type-text">string</span></td>
  <td>Account name.</td>
</tr>
<tr>
  <td>`displayOrder` <br /><span class="type-text">integer</span></td>
  <td>Display order.</td>
</tr>
</table>
</details>

</div>
<div class="child2">

<Tabs>
<TabItem value="JSON" label="JSON" default>

```json title="Request sample"
{
  "pageNo": 1,
  "noOfEntries": 20,
  "searchField": "Sample text",
  "dataProviderIds": [
    1
  ],
  "healthcareSystemIds": [
    1,
    2,
    3
  ]
}
```

```json title="Response 200"
{
  "success": true,
  "data": {
    "accountNameList": [
      {
        "id": 1,
        "displayName": "Account Name 1",
        "displayOrder": 1
      },
      {
        "id": 2,
        "displayName": "Account Name 2",
        "displayOrder": 20
      }
    ],
    "filteredRecords": 5,
    "totalRecords": 3294
  }
}
```

</TabItem>
<TabItem value="TypeScript" label="TypeScript">

See [TypeScript prerequisites](/getting-started/typescript-prerequisites.mdx) for usage. 

```ts
import {
  getInstance
} from "prerequisites"

const axios = getInstance();

interface Responses {
  200: {
    content: {
      "application/json": {
        success: boolean;
        data: {
          accountNameList: {
            id: number;
            displayName: string;
            displayOrder: number;
          }[];
          filteredRecords: number;
          totalRecords: number;
        }
      };
    };
  };
}

function getAccountNameList(): Promise<Responses> {
  const options = {
    method: 'POST',
    url: 'https://api.iqm.com/api/v3/audience/abm/healthcare-data/account-names',
    requestBody: {
      content: {
        "application/json": {
          searchBy?: `string[]`,
          ids?: `number[]`,
          dataProviderIds?: `number[]`,
          verticalId: `number`,
          healthCareSystemIds?: `number[]`,
          accountNameIds?: `number[]`,
          accountTypeIds?: `number[]`,
          pagination?: `string`,
          pageNo?: `number`,
          noOfEntries?: `number`,
          sortBy?: `string`,
          searchField?: `string`,
          order?: `ASC` | `DESC`,
          offset?: `number`,
        }
      }
    }
  }

  return axios.request(options).then(({ data }: { data: Responses }) => data);
}
```

</TabItem></Tabs>

</div></div>